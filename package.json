{
  "name": "onion-architect",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @onion/cli dev",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "fs-extra": "^11.3.3"
  }
}
